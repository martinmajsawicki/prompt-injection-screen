PyMuPDF>=1.24       # Layer 0: PDF glyph inspection (color, size, position), metadata
python-docx>=1.1    # Layer 0: hidden DOCX runs, metadata
beautifulsoup4>=4.12 # Layer 0: hidden HTML (CSS, comments, alt/aria)
# Layer 1 (OpenRouter) uses only the standard library (urllib) — no extra dependencies.
