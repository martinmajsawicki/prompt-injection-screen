# Python environment (machine-specific, large) — recreated from requirements.txt
.venv/
__pycache__/
*.pyc

# Test fixtures are GENERATED by tests/run_all.py — not versioned
tests/fixtures/

# Sanitized copies and scan outputs
*.clean.md
*.sanitized.md

# Secrets — NEVER commit the API key (kept outside the repo: env / ~/.claude/settings.json)
.env
*.key

# System
.DS_Store
